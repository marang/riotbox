Riotbox Jam TR-909 Render Diagnostics Baseline
Normalized terminal capture notes for `RIOTBOX-44`
Source: `riotbox-app` shell state fixture
Notes:
- normalized from the current Jam and Log shell state fixture
- intended as a review/update artifact, not a pixel-perfect golden render
- highlights read-only audible render diagnostics without opening a new TR-909 control path

Key shell cues after this slice:

- `Jam -> Lanes` now includes concise render diagnostics:
  - `909 mode hybrid | render takeover`
  - `drum_bus_takeover | controlled_phrase`
  - `scene-a-main | drum 0.82 | slam 0.90`
- `Log -> TR-909 Render` now includes:
  - `render takeover via drum_bus_takeover`
  - `controlled_phrase | scene-a-main`
  - `drum 0.82 | slam 0.90 | takeover aligned`

Interpretation:

- the Jam screen now exposes enough audible render state to understand what the lane is asking the audio seam to do
- the Log screen now shows the same render contract in a compact diagnostic panel for troubleshooting and review
- diagnostics remain derived from committed lane state and the existing audio-facing render seam; no parallel TR-909 state path was introduced
