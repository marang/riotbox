# RIOTBOX-1465 Dense Foundation Event-Context v3 Development Rejection

Date: 2026-08-25

Work class: `audible_vertical_slice / development_exploration`

Outcome: frozen v3 rejected before render or listening because its two
role-matched event substitutions preserved clarity but did not create the
required material answer contrast

## Frozen Boundary

RIOTBOX-1465 was the third and final causal topology for the current Dense
foundation answer role. Before fresh source access, RBX-327 froze
`riotbox.dense_break_foundation_chop.v3` as a materially different application
of research hypothesis H-CHOP-1: source-derived event variants are selected by
source-relative role and accent rank while target onsets stay unchanged.

The selector reuses the pinned Stage-A v2 detector and complete event anatomy.
It keeps beats zero through five sample-exact, then maps one `upper_attack` and
one `low_body` event in beats six through seven to unique same-role donors from
the first six beats. Complete target and donor anatomy, a 20 ms onset
lookbehind, 2 ms boundary fades, crosstalk refusal, clarity limits, and material
contrast limits are all frozen. No EQ, gain repair, generated support,
pitch/time change, bus treatment, fallback music, or source-specific exception
is permitted.

## Source-Blind Falsification

Generated stereo fixtures pass before source access. They prove that the
selector:

- reuses the frozen detector, physical onset, anatomy, and composite ownership;
- derives two clean source-relative event roles;
- selects two unique nearest-accent same-role donors;
- preserves the first six beats sample-exactly;
- preserves the target onset offset exactly; and
- rejects near-identical within-role variants rather than presenting a false
  musical change.

The source-blind fixture's worst local low-band and level changes remain below
`0.60 dB`, while normalized RMS delta `0.506` and correlation `0.871` prove
that the synthetic mechanism can be both clear and materially distinct.

## Bounded Development Result

After contract and Decision freeze, one fresh exclusive
`DevelopmentSourceAccessSession` opened only registered case
`dense_beat03_130` by its exact path and expected SHA-256. The access layer
performed no directory discovery and opened no Holdout audio. The verified
bytes were delivered in process to the qualification owner.

The detector found 33 NMS events and admitted 14 complete isolated contexts.
Their low-band shares formed a clean role split: the selected gap was `0.605`
across a total span of `0.713`. Both target roles received unique donors with
accent-rank distance one. Donor-to-target context differences were small:
absolute low-band delta stayed at or below `0.494 dB`, absolute level delta at
or below `0.215 dB`, and absolute attack-level delta at or below `0.303 dB`.

The resulting answer preserved weight and clarity:

- whole-answer low-band delta: `-0.030 dB`;
- whole-answer level delta: `-0.085 dB`;
- worst absolute half-beat low-band delta: `0.500 dB`; and
- worst absolute half-beat level delta: `0.222 dB`.

It nevertheless failed both frozen material-contrast gates. Normalized RMS
delta was `0.1656`, below the required `0.20`, and absolute waveform
correlation was `0.9863`, above the allowed `0.96`. The owner therefore raised
`answer_clarity_or_contrast_gate_failed`. The session recorded an audited
abort and emitted only a JSON rejection record; no source-loop WAV, candidate
WAV, presentation, or playback was created.

## Human Listening

No listening review occurred. The exact candidate was ineligible and was not
rendered, so there was no honest musician-facing A/B question to ask. This is
automated negative evidence about insufficient causal contrast, not a human
verdict.

## Consequence

Frozen v3 is rejected. It demonstrates that complete role- and accent-matched
event substitution can avoid the radio-like clarity loss that rejected v1,
but on the registered Development source it is too close to the original to
serve as a meaningful foundation answer. The result does not permit threshold
loosening, source substitution, more event replacements, or v4 tuning of the
same role and selector.

The current Dense foundation-answer role is closed after three bounded causal
topologies. A future attempt requires a different musical owner or grammar,
a new Linear-first issue, and a new version and Decision frozen before any
fresh source access. Dense remains non-demo-ready. V3 grants no product,
source-general, quality, hardness, Holdout, demo, release,
automatic-arrangement, or P023-completion claim.

## Evidence Identities

- frozen v3 contract:
  `7bcd88833101988fbb08b9761d169f234163abc40f1f9524ca5d478a597248a2`
- retained source-blind selector/fixture implementation:
  `f52434f81ae9d07f817addffced0b94c6a77322d176a064afc3758776ff476fc`
- fail-closed technical rejection:
  `81b2c8e8f641b0d849f3f32aecaf5421455f6ba2983c3c32b0c2114b542cbb1b`
- failed-closed Development access log:
  `bfdce976b4e39df2fa3a9c4e91fbc8bd290eca0073b1d5d3bac5af0678682b84`
