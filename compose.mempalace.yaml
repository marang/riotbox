services:
  mempalace:
    build:
      context: .
      dockerfile: Containerfile.mempalace
    network_mode: none
    working_dir: /repo
    volumes:
      - .:/repo
      - ./.mempalace/palace:/palace
      - ./.mempalace/cache:/root/.cache
